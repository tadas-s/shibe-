#
# Development only, open-trust-everyone access configuration
#

# TYPE  DATABASE    USER        CIDR-ADDRESS          METHOD
local   all         all                               trust
host    all         all         127.0.0.1/32          trust
host    all         all         10.0.0.1/24          trust
